[#ftl]
[@b.head/]
[@b.toolbar title="住院押金"]bar.addBack();[/@]
  [@b.form action=b.rest.save(deposit) theme="list"]
    [#if deposit.persisted]
      [@b.field label="病人"]
        ${deposit.inpatient.code} ${deposit.inpatient.name}
        [@b.field label="金额"]${deposit.amount}元[/@]
        [@b.field label="入院时间"]${deposit.inpatient.beginAt?string("yyyy-MM-dd")}[/@]
      [/@]
    [#else]
      [@b.textfield name="deposit.inpatient.code" label="住院号" value="" required="true" maxlength="10"/]
    [/#if]
    [@b.textfield name="deposit.amount" label="金额" value=deposit.amount! required="true" comment="元"/]
    [@b.date name="deposit.payAt" label="缴费日期" value=deposit.payAt! required="true" format="datetime" /]
    [#if deposit.persisted]
      [@b.date name="deposit.refundAt" label="退回日期" value=deposit.refundAt! required="false" format="datetime"/]
    [/#if]
    [@b.formfoot]
      [@b.reset/]&nbsp;&nbsp;[@b.submit value="action.submit"/]
    [/@]
  [/@]
[@b.foot/]
